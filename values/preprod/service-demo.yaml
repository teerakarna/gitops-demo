# Updated automatically by cd.yml on every merge to main.
image:
  tag: latest   # replaced with main-{sha} by CD pipeline

env:
  ENV_NAME: preprod

servicesNamespace: preprod

replicaCount: 2

resources:
  limits:
    cpu: 250m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 32Mi
